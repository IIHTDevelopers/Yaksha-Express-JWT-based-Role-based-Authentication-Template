const jwt = require('jsonwebtoken');
const User = require('../models/user');

const jwtSecret = 'your_jwt_secret_key';

// User registration
exports.registerUser = null;

// User login (generates JWT)
exports.loginUser = null;
